server:
  port: 8000
  servlet:
    context-path: /
    session:
      cookie:
        name: ueboot-shiro
spring:
  cache:
    type: none
    cache-names: systemCache
  session:
    store-type: none
  servlet:
    multipart:
      enabled: true
      max-file-size: 500MB
      max-request-size: 500MB
  jpa:
    open-in-view: true
    database: mysql
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57InnoDBDialect
        format_sql: false
      jadira:
        usertype:
          autoRegisterUserTypes: true
          javaZone: Asia/Shanghai
          databaseZone: Asia/Shanghai
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://rm-uf69mw16qpk0z2y0nio.mysql.rds.aliyuncs.com:3306/apartment_test?characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: apartment_h_dev
    password: HsvBLuGuCAo2IrzK
    tomcat:
      max-wait: 500
      max-active: 200
      initial-size: 5
      max-idle: 5
      min-idle: 5
  redis:
    host: 127.0.0.1
    password:
    port: 6379
    database: 3
    timeout: 60s
  data:
    redis:
      repositories:
        enabled: false #设置为false,防止启动时提示Multiple Spring Data modules found, entering strict repository configuration mode
